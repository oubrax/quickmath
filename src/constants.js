export const HISTORY_STORAGE_KEY = "quickmath.history.v1";
export const OUTPUT_MODE_STORAGE_KEY = "quickmath.outputMode.v1";

export const MAX_HISTORY_ITEMS = 50;
export const MAX_SOLUTIONS_DISPLAY = 20;

export const DEFAULT_GRAPH_VIEW = Object.freeze({
  xMin: -10,
  xMax: 10,
  yMin: -10,
  yMax: 10,
});
